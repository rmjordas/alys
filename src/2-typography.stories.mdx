import { Meta, Typeset } from '@storybook/addon-docs/blocks';

import { typography } from './styles/themes/light';

<Meta title="Getting Started/Typography" />

# Typography

**Font:** Inter

**Weights:** 100 (thin), 200 (extraLight), 300 (light), 400 (regular), 500 (medium), 600 (semiBold), 700 (bold), 800 (extraBold), 900 (black)

<Typeset
  fontSizes={[
    typography.size.s1,
    typography.size.s2,
    typography.size.s3,
    typography.size.m1,
    typography.size.m2,
    typography.size.m3,
    typography.size.l1,
    typography.size.l2,
    typography.size.l3,
  ]}
  fontWeight={typography.weight.regular}
/>
