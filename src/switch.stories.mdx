import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { Fragment } from 'react';

import { Switch } from './switch';

<Meta title="Components/Switch" component={Switch}></Meta>

# Switch

A toggle switch can be used to provide the user an input for something represented by an on and off
state. It is similar to a checkbox except it normally does not allow an intermediate state.

> "Only a Sith deals in absolutes." - A Jedi Master stating an absolute

## Overview

Switches are commonly used inside forms. Accessibilty practices require that it must always be
provided with a label, even though the label is hidden by default.

<Story name="Demo">
  <Switch label="My label" />
</Story>

<Props of={Switch} />

## Stories

<br />

### Default

<Preview>
  <Story name='Default'>
    <Switch label="Toggle Switch" />
  </Story>
</Preview>

### Checked

<Preview>
  <Story name='Checked'>
    <Switch checked label="Toggle Switch" />
  </Story>
</Preview>

### Disabled

Adding a `disabled` attribute prevents interaction with the toggle switch.

<Preview>
  <Story name='Disabled'>
    <Switch disabled label="Toggle Switch" />
  </Story>
</Preview>

### Disabled (checked)

Adding a `disabled` attribute prevents interaction with the toggle switch.

<Preview>
  <Story name='Disabled but checked'>
    <Switch disabled checked label="Toggle Switch" />
  </Story>
</Preview>

### Show label

To show the label beside the toggle switch, add the `showLabel` boolean attribute to the toggle
switch.

<Preview>
  <Story name='Default with label shown'>
    <Switch showLabel label="Toggle Switch" />
  </Story>
</Preview>

### Show label (disabled)

Adding a `disabled` attribute prevents interaction with the toggle switch.

<Preview>
  <Story name='Disabled with label shown'>
    <Switch disabled showLabel label="Toggle Switch" />
  </Story>
</Preview>

### Size (small)

<Preview>
  <Story name='Small size'>
    <Switch size="small" label="Toggle Switch" />
  </Story>
</Preview>

### Size (small with label shown)

<Preview>
  <Story name='Small size with label'>
    <Switch size="small" showLabel label="Toggle Switch" />
  </Story>
</Preview>

### Inline by default

<Preview>
  <Story
    name='As inline element'
    decorators={[
      (StoryFn) => (<div><StoryFn /></div>)
    ]}
  >
    <Fragment>
      <Switch showLabel label="Toggle Switch" /> This text is not part of the Switch label
    </Fragment>
  </Story>
</Preview>
